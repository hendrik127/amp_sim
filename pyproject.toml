[project]
name = "amp-sim"
version = "0.1.0"
description = "guitar amplifier simulation using neural networks"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
